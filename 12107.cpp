#include <iostream>
using namespace std;


int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    int N;

    cin >> N;
    if(N!=1) cout << "A" << '\n';
    else cout << "B" << '\n';
    return 0;
}