#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;


int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    // int N;
    // int a[2][250000];
    // cin >> N;
    // for(int i = 0 ; i < N ; i ++){
    //     cin >> a[0][i] >> a[1][i];
    // }
    int N;
    int arr[500001];
    cin >> N;
    for(int i = 0 ; i < N ; i ++){
        int a,b;
        arr[a] = 1;
        arr[b] = 1;
    }

    return 0;
}