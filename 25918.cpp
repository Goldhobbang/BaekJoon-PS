#include <iostream>
using namespace std;


int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    int N;
    char S[200001];

    cin >> N;
    for(int i = 0 ; i < N; i++){
        cin >> S[i];
    }
    
    return 0;
}